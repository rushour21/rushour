import { ComingSoon } from "@/components/app/coming-soon";

export default function Page() {
  return <ComingSoon eyebrow="Habits" title="Small things, repeated" subtitle="Cue, action, result — tracked for drift, not streaks." />;
}
